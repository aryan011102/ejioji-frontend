<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>theonebytwo</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>theonebytwo</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- Portrait only, matching the lock set in main.dart. A profile rotated
	     to landscape is a wall of half-height tiles, and none of the layouts
	     were drawn for it. Declaring it here as well stops the rotation
	     animation happening before Dart refuses it.

	     iPhone only (TARGETED_DEVICE_FAMILY = 1): App Store Connect refuses an
	     iPad build that does not allow every orientation, for multitasking.
	     An iPad still runs the app, in its iPhone mode. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<!-- This app is dark only. Without this, UIKit renders the keyboard, the
	     date picker and the share sheet in light mode on a device that has
	     dark mode off, in the middle of a black app. -->
	<key>UIUserInterfaceStyle</key>
	<string>Dark</string>

	<!-- Why the photo picker is being opened. iOS shows this sentence
	     verbatim, so it is written for the person rather than for the
	     reviewer. -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>To put photos on your profile. We only receive the ones you pick.</string>

	<!-- Never asked today: photos come from the library and files from the
	     Files app. But image_picker carries camera and video-recording code
	     and file_picker carries music-library code, and App Store Connect
	     silently drops a build that links those without a purpose string. -->
	<key>NSCameraUsageDescription</key>
	<string>To take a photo to send or put on your profile.</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>To record sound with a video you send.</string>
	<key>NSAppleMusicUsageDescription</key>
	<string>If you connect Apple Music, to read the songs and albums in your library once for your music insights. Also to pick a file you choose to share.</string>

	<!-- url_launcher checks whether it can open a link before opening it, and
	     on iOS that check needs the scheme declared. Only https: the consent
	     pages are the only thing this app opens. -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>https</string>
	</array>

	<!-- The only encryption in the app is the operating system's HTTPS, which
	     is exempt. Saying so here stops App Store Connect asking the export
	     compliance question on every TestFlight upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- The OAuth return link (https://theonebytwo.com/oauth/google) is read
	     by app_links in ConnectController, and only there. Left on, Flutter's
	     own deep linking also hands it to the router, which has no such page:
	     a "Page Not Found" covered the connect screen, and the push to the
	     reading screen that follows never showed. -->
	<key>FlutterDeepLinkingEnabled</key>
	<false/>

	<!-- A push that arrives while the app is in the background is handed to it
	     rather than only to the lock screen, which is what lets a tap open the
	     conversation it is about. The notification itself is drawn by iOS; the
	     app never wakes up to fetch anything. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
</dict>
</plist>
